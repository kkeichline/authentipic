'''
Download data from various sources and split it into train, validation, and test sets for a CNN model
Start with FaceForensics++ dataset
'''

import os
import logging
import pandas as pd
from typing import Tuple


def download_data(url: str, output_path: str):
	pass

def split_data(data_path: str, train_ratio: float, val_ratio: float):
    # Split data into train, validation, and test sets
	pass